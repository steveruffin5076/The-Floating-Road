{
  "name": "the-floating-road",
  "private": true,
  "version": "0.0.1",
  "description": "The Floating Road - vertical slice (Act 1, The Masterless Blade). See game-plan/ for design docs.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "preview": "vite preview",
    "sim": "vite-node src/sim/run.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
